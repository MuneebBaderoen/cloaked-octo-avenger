// var Layer = require('./layer.js');

describe('Tests suite for Layer.js', function () {
    beforeEach(function () {

    })

    it('should do something', function () {
        expect(1)
            .toBe(3);
        expect(1)
            .toBe(2);
    });

    afterEach(function () {


    })
});
